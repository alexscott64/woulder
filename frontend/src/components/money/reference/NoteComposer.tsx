import { useState } from 'react';
import { Camera, FileText, PenLine } from 'lucide-react';
import { MoneyCragNode, MoneyNoteBlock, MoneyNoteTargetType } from '../../../types/money';
import { flattenAreas, flattenBoulders } from './model';
import { T } from './theme';

export function NoteComposer({ root, area, boulder, initialBlock, mobile, onClose, onSubmit }: { root: MoneyCragNode | null; area: MoneyCragNode; boulder: MoneyCragNode | null; initialBlock?: 'photo' | 'sketch' | 'file' | null; mobile: boolean; onClose: () => void; onSubmit: (payload: { body: string; tags: string[]; target_type: MoneyNoteTargetType; target_ref?: string; blocks: MoneyNoteBlock[]; files: Array<{ file: File; kind: 'photo' | 'file' }> }) => void }) {
  const [body, setBody] = useState('');
  const [tags, setTags] = useState('');
  const [blocks, setBlocks] = useState<MoneyNoteBlock[]>(initialBlock === 'sketch' ? [{ kind: 'sketch', name: 'field-sketch.png' }] : []);
  const [files, setFiles] = useState<Array<{ file: File; kind: 'photo' | 'file' }>>([]);
  const [target, setTarget] = useState(boulder ? `boulder:${boulder.feature.id}` : `area:${area.feature.id}`);
  const areas = flattenAreas(root);
  const boulders = flattenBoulders(root);
  const addFile = (file: File, kind: 'photo' | 'file') => { setFiles(f => [...f, { file, kind }]); setBlocks(b => [...b, { kind, name: file.name, url: kind === 'photo' ? URL.createObjectURL(file) : undefined }]); };
  const canPost = Boolean(body.trim() || blocks.length);
  const post = () => { const [type, id] = target.split(':'); onSubmit({ body: body.trim(), tags: tags.split(',').map(s => s.trim()).filter(Boolean), target_type: type as MoneyNoteTargetType, target_ref: id, blocks, files }); };
  return <div onClick={onClose} style={{ position: 'fixed', inset: 0, zIndex: 70, background: 'rgba(8,5,4,0.62)', display: 'flex', alignItems: mobile ? 'flex-end' : 'center', justifyContent: 'center' }}><div onClick={e => e.stopPropagation()} style={{ background: T.surf, border: `1px solid ${T.line2}`, borderRadius: mobile ? '18px 18px 0 0' : 16, boxShadow: T.shadow, width: mobile ? '100%' : 460, maxHeight: '94vh', overflowY: 'auto' }}><div style={{ padding: mobile ? '4px 18px 20px' : 22 }}><div style={{ display: 'flex', alignItems: 'center', marginBottom: 14 }}><span style={{ fontSize: 18, fontWeight: 800, color: T.ink }}>New note</span><button onClick={onClose} style={{ marginLeft: 'auto', border: 'none', background: 'transparent', color: T.mut, cursor: 'pointer', fontSize: 20 }}>×</button></div><Label>Link to</Label><select value={target} onChange={e => setTarget(e.target.value)} style={inp}><option value="none:">No link · general log</option>{areas.map(a => <option key={a.feature.id} value={`area:${a.feature.id}`}>{a.feature.title}</option>)}{boulders.map(b => <option key={b.feature.id} value={`boulder:${b.feature.id}`}>◈ {b.feature.title}</option>)}</select><textarea value={body} onChange={e => setBody(e.target.value)} placeholder="What did you find? Beta, conditions, an idea…" rows={mobile ? 3 : 4} style={{ ...inp, resize: 'vertical', lineHeight: 1.5, marginTop: 12 }} />{blocks.length > 0 && <div style={{ display: 'flex', flexWrap: 'wrap', gap: 8, margin: '12px 0' }}>{blocks.map((bl, i) => <div key={i} style={{ position: 'relative' }}>{bl.url ? <img src={bl.url} alt="" style={{ width: 72, height: 72, objectFit: 'cover', borderRadius: 8, border: `1px solid ${T.line2}` }} /> : <div style={{ display: 'flex', alignItems: 'center', gap: 6, height: 72, padding: '0 12px', background: T.surf2, border: `1px solid ${T.line2}`, borderRadius: 8, color: T.ink, fontSize: 12, maxWidth: 150 }}><FileText size={16} />{bl.name}</div>}<button onClick={() => setBlocks(x => x.filter((_, j) => j !== i))} style={{ position: 'absolute', top: -7, right: -7, width: 20, height: 20, borderRadius: '50%', border: `1px solid ${T.line2}`, background: T.raise, color: T.ink }}>×</button></div>)}</div>}<div style={{ display: 'flex', gap: 8, margin: '14px 0' }}><UploadButton accept="image/*" icon={<Camera size={19} />} label="Photo" onFile={f => addFile(f, 'photo')} /><button onClick={() => setBlocks(b => [...b, { kind: 'sketch', name: 'field-sketch.png' }])} style={addBtn}><PenLine size={19} color={T.accent} />Sketch</button><UploadButton icon={<FileText size={19} />} label="Attach" onFile={f => addFile(f, 'file')} /></div><input value={tags} onChange={e => setTags(e.target.value)} placeholder="tags (comma separated) — optional" style={{ ...inp, fontFamily: T.mono, fontSize: 12 }} /><div style={{ display: 'flex', gap: 8, marginTop: 16 }}><button onClick={post} disabled={!canPost} style={{ flex: 1, border: 'none', borderRadius: 10, padding: 13, background: canPost ? T.accent : T.line2, color: canPost ? T.onAccent : T.faint, fontWeight: 700 }}>Post note</button><button onClick={onClose} style={{ border: `1px solid ${T.line2}`, borderRadius: 10, padding: '13px 18px', background: 'transparent', color: T.ink, fontWeight: 700 }}>Cancel</button></div></div></div></div>;
}
function Label({ children }: { children: React.ReactNode }) { return <div style={{ fontFamily: T.mono, fontSize: 10.5, color: T.faint, textTransform: 'uppercase', letterSpacing: 0.5, marginBottom: 6 }}>{children}</div>; }
const inp: React.CSSProperties = { width: '100%', background: T.surf2, border: `1px solid ${T.line2}`, borderRadius: 9, padding: '10px 12px', color: T.ink, fontFamily: T.font, fontSize: 14, outline: 'none', boxSizing: 'border-box' };
const addBtn: React.CSSProperties = { flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 5, border: `1px solid ${T.line2}`, background: 'transparent', color: T.mut, borderRadius: 10, padding: '11px 6px', minHeight: 58, cursor: 'pointer', fontWeight: 700, fontSize: 11.5 };
function UploadButton({ icon, label, accept, onFile }: { icon: React.ReactNode; label: string; accept?: string; onFile: (file: File) => void }) { return <label style={addBtn}>{icon}{label}<input type="file" accept={accept} style={{ display: 'none' }} onChange={e => { const file = e.currentTarget.files?.[0]; if (file) onFile(file); e.currentTarget.value = ''; }} /></label>; }
